name: datacleanenv-x
version: 1.0.0

entry_point: env.core:DataCleanEnv

tasks:
  - name: easy
  - name: medium
  - name: hard

api:
  reset: /reset
  step: /step
  state: /state

models:
  observation: ObservationModel
  action: ActionModel
  reward: RewardModel
